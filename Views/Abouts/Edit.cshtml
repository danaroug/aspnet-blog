@model WarbandOfTheSpiritborn.Models.About

@{
    ViewData["Title"] = "Edit About Content";
}

<link href="https://cdn.quilljs.com/1.3.7/quill.snow.css" rel="stylesheet" />

<partial name="_HelperNav" model='"About"' />

<div class="main-container">
    <div class="container-fluid">
        <section class="mb-4">
            <h1>Edit About Content</h1>
            <p class="text-muted">
                Update this About page section using the rich text editor.
            </p>
        </section>

        <form asp-action="Edit" asp-route-id="@Model.Id" method="post" id="aboutForm" class="about-editor-card">
            <input type="hidden" asp-for="Id" />

            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="form-group mb-3">
                <label asp-for="AboutTitle" class="control-label pb-2">Title</label>
                <input asp-for="AboutTitle" class="form-control" />
                <span asp-validation-for="AboutTitle" class="text-danger"></span>
            </div>

            <div class="form-group mb-4">
                <label asp-for="AboutText" class="control-label pb-2">Content</label>

                <div id="aboutEditor" class="about-quill-editor"></div>

                <textarea asp-for="AboutText" id="AboutText" class="d-none"></textarea>
                <span asp-validation-for="AboutText" class="text-danger"></span>
            </div>

            <div class="form-group d-flex gap-2">
                <input type="submit" value="Save" class="btn btn-primary btn-dark" />
                <a asp-action="Index" class="btn btn-outline-secondary">Back</a>
            </div>
        </form>
    </div>
</div>

<style>
    .about-editor-card {
        padding: 1.5rem;
        border-radius: 0.75rem;
        background: rgba(255, 255, 255, 0.03);
    }

    .about-quill-editor {
        min-height: 260px;
        background: #fff;
        color: #212529;
    }

    .ql-toolbar.ql-snow {
        border-radius: 0.5rem 0.5rem 0 0;
    }

    .ql-container.ql-snow {
        border-radius: 0 0 0.5rem 0.5rem;
    }
</style>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script src="https://cdn.quilljs.com/1.3.7/quill.min.js"></script>

    <script>
        const toolbarOptions = [
            [{ 'header': [1, 2, 3, false] }],
            ['bold', 'italic', 'underline'],
            [{ 'list': 'ordered' }, { 'list': 'bullet' }],
            ['blockquote', 'link'],
            ['clean']
        ];

        const existingAboutText = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.AboutText ?? string.Empty));

        const quill = new Quill('#aboutEditor', {
            theme: 'snow',
            placeholder: 'Write the About page content here...',
            modules: {
                toolbar: toolbarOptions
            }
        });

        quill.root.innerHTML = existingAboutText;

        const aboutForm = document.getElementById('aboutForm');
        const aboutTextInput = document.getElementById('AboutText');

        aboutForm.addEventListener('submit', function () {
            const editorText = quill.getText().trim();

            // Store an empty value when the editor has no real content.
            aboutTextInput.value = editorText.length === 0 ? '' : quill.root.innerHTML;
        });
    </script>
}
